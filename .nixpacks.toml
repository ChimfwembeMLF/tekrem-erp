[phases.setup]
nixPackages = ["php83", "composer"]

[phases.install]
cmds = [
    "composer install --no-interaction --optimize-autoloader --no-dev"
]

# Skip Node.js/Yarn installation entirely
[build]
excludeNodeModules = true

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=8080"